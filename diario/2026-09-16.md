# 16/09/2026 — Saldo projetado com atrasados, novo logo, Ponto com ciclo e Patrimônio em Relatórios

## 1. Saldo projetado inclui previstos atrasados (bug 16/09, semana 38 PJ)

Sintoma: card mostrava -R$ 875,49 enquanto o detalhe da semana marcava +R$ 1.388,95 — divergência porque `projetarSerie` filtrava só `data_prevista > hoje`, assumindo que tudo com `data <= hoje` já estava no `saldoAtual`. Vale para `realizado`, mas não para `previsto` pendente (atrasado): ele não gerou movimentação e precisa entrar na projeção.

- **`src/lib/saldoProjetado.js:88`**: além dos futuros (`> hoje`), inclui `data <= hoje` com `estado === 'previsto'` (pendente). `realizado` com data passada continua fora (já está no saldo real, senão duplicaria — bug de 11/09). Comentário de cabeçalho e de `useSaldoProjetado` atualizados para a nova regra com exceção do atrasado.
- **`scripts/teste_saldoProjetado.mjs`**: `T8`/`T9` ajustados para `estado: realizado` nos itens “já no saldo” e novos `T12` (atrasado pendente entra), `T13` (realizado passado não entra) e `T14` (futuro continua) — 23/23. `node` ok.

## 2. Novo logo ANPEREZ

Imagem quadrada 1024 com cantos arredondados embutidos, fundo navy e lockup completo. Para não borrar o texto em tamanhos pequenos e não conflitar com o `maskable` do Android:

- **`src/assets/logo.png` / `anperez-logo.png`** (emblema 256, 54 KB vs 520 KB/1 MB antes, crop 240,110–784,620 + brilho/contraste),
- `public/favicon.ico` (emblema 16/32/48 em fundo navy),
- `public/apple-touch-icon.png` (lockup 180, pode manter texto),
- `public/icon-192.png` / `icon-512.png` (lockup 192/512),
- `public/icon-512-maskable.png` (emblema 60% em fundo sólido navy com safe-area 20%, sem cantos arredondados).

Peso caiu de >1,5 MB para ~300 KB totais.

### Ajustes finos do logo (após screenshots)

Emblema apagado em 30 px: `Layout.jsx:341` header `30→40 px` + borda `rgba(255,255,255,0.08)` e sombra, `Dashboard.jsx:265` `38→44 px`, `Login.jsx:117` `72→84 px`; PNG regenerado com `fill 88%` e `+12% brilho / +15% contraste` (82 KB). `Dashboard:brandCenter/brandName/brandSub` com `nowrap` para não cortar “MONEY”. `manifest` e `favicon` com `?v=20260916` para forçar atualização do PWA em desktop (ícone antigo cacheado mesmo após desinstalar).

## 3. Ponto Inteligente na Home com ciclo (mesmo padrão Contas/Cartões)

Substitui o “+0h” fixo por até três visões derivadas de `pontoCalc.js` sem duplicar regra:

- `visoesPontoHome({excecoes,inicio,fim,hoje,feriados,ferias})` → `cargaTotal` (esperada da semana), `cumprida` (base dos dias já passados + hoje se já cumpriu — `diaJaCumprido` via `turnoPadrao`/`horaEmMinutos` com virada — + `he`/`horasDomfer` até hoje), `restante = cargaTotal - baseCumprida`, `extras = he` total, `semanaFechada = fim < hoje` (mesmo `semanaFechada` da reconciliação).
- `useResumoPonto` expõe `visoes`; `Dashboard.jsx:42` ciclo com `useState` + `%` e `aoClicarValor` (igual a `modoContas`/`modoCartoes`): `cumprida → restante → extras` com regras `restante` sai se fechada, `extras` só se `>0` (4 combinações). 57 testes `pontoCalc` intactos, build 375 ok. Não commitado sem revisão até aprovação, depois `3ff6156`.

## 4. Aba Patrimônio em Relatórios e cálculo unificado

- **`src/lib/patrimonioCalc.js` (nova)**: `calcularPatrimonioAtual` (contas ativas + caixinhas ativas) usado também para corrigir o card de Patrimônio em `ContasCorrentes.jsx:40` e `Dashboard.jsx:66` (antes duplicado); `patrimonioEmData` faz replay reverso por conta via `calcularSaldoReal` + caixinhas constantes (limitação assumida — `caixinha_movimentacoes` existe mas sem histórico retroativo confiável, caixinhas constantes); `calcularEvolucaoPatrimonio` gera pontos por bucket com granularidade `dia` (Semana/Mês, até 31 barras), `semana` (Trimestre) e `mes` (Semestre/Ano; Personalizado por duração) e variação R$/%.

- **`src/hooks/useRelatorioPatrimonio.js` (novo)**: busca `contas` + `caixinhas` + `movimentacoes` (cobertura 400d) e devolve `cards` (Patrimônio atual, Variação R$, Variação %), `grafico` (`rotulos`/`valores`) e `linhas` para `RelatorioTemplate`.

- **`src/pages/Relatorios.jsx:16`**: nova aba `patrimonio` com `useRelatorioPatrimonio` no mesmo `RelatorioTemplate`.

## 5. Correções pós-revisão do Patrimônio (não commitadas ainda)

- **Replay travado**: `dez/2025` vinha reto porque `historico_*` (250 `historico_planilha` + 136 `acordo` + 4 `outros`, só `Entrada`, de jan/2021 a ago/2026) não entrava no replay. Hook agora busca `planejamentos` histórico (`origem in (...)`, `estado=realizado`) e `patrimonioCalc` mescla com corte `2026-01-01` para não dobrar com `movimentacoes` (que só existe de jan/2026, 517 linhas). `dez/2025` agora varia (5 valores únicos, 491→501k) e `2024 Ano` 12 meses varia 283k→369k.

- **Gráfico estourando**: `RelatorioTemplate.jsx:258` 31 barras diárias estouravam a largura. `cardGrafico` com `overflowX:auto` e `barras`/`eixo` com `minWidth:max-content` + `coluna` `14px`; depois isolado para só ativar quando `qtdBarras > 12` (Patrimônio diário precisa, 2 barras de *Recebido* e 5 de *Acordo* voltam a esticar).

Validação: `npm run build` 375 ok, `dez/2025` e `ago/2026` variando; pendente checagem de unidade (histórico em `numeric(12,2)` reais, mesma de `movimentacoes`) e do conceito de patrimônio no histórico puro (soma cumulativa desde zero = fluxo acumulado, não saldo guardado — sem saldo inicial real de 2021, valor absoluto fica grande; variação continua válida, absoluto é aproximação sinalizada no código).

## Verificação

- `node scripts/teste_pontoCalc.mjs` 57 ok, `teste_saldoProjetado` 23 ok, `teste_patrimonio` sintético ok, `npm run build` 375 ok em todas as etapas.
