# 19/09/2026 — Pendência de atraso (migration 38), saldo projetado sem duplicar realizado futuro, renda sobre entradas + saldo anterior e recebido parcial no relatório/planilha

## 1. Pendência de atraso (migration 38, caminho 3 — clique explícito)

Previsto atrasado agora pode migrar: parcial nasce **dentro da `realizar_planejamento`** (mesma transação — depois do UPDATE a original já é `realizado` e o lazy nunca a acharia) e total via botão **"Jogar p/ próx. semana"** com confirm (`Mover R$X (previsto de dd/mm) pra dd/mm?`).

- **`supabase/38_pendencia_atraso.sql` (nova, aplicada)**: colunas `origem_atraso_id`, `valor_pendente`, `nota_pendencia`; estado `migrado`; `UNIQUE` parcial em `origem_atraso_id`; `realizar_planejamento` com `p_nota_pendencia`/`p_data_pendente` (só gera pendência se sobra > 0 **e** `data_prevista <= data da realização — parcial de previsão futura não migra); `migrar_atraso` idempotente (`NOT EXISTS` + `ON CONFLICT DO NOTHING`). Pendente sempre avulsa (`serie_id` nulo, `origem manual`) para não contaminar média móvel nem reconciliação do Ponto.
- **Campo de nota** no modal Lançar, só quando `0 < valor < previsto` em ocorrência atrasada; canônico na original, cópia de exibição na tag da pendente.
- **`pendenciaAtraso.js` + `teste_pendenciaAtraso` 6/6**: próxima da série (mensal com clamp, semanal +7) ou próxima segunda para avulso.
- **Corrida lazy**: o `useEffect` de montagem foi removido antes de qualquer teste real — a "migração automática" vista no teste era resíduo da rodada anterior do lazy (pendente com `criado_em` 18/09 19:59, anterior ao caminho 3), confirmado por varredura (sem triggers/cron/functions no banco, 1 processo vite, 1 chamador no front) + reset das linhas de teste + reteste com Network. Sem commit até essa confirmação.
- Badges `Migrado` (lilás) e `Pendente` (âmbar), `migrado` fora de totais/saldo/renda/regen.

## 2. Saldo projetado: realizado com data futura não entra (T15)

Sintoma: semana com 0 previstos + 2 realizados, saldo -R$ 593,49 em vez de R$ 306,51 (`306,51 − 900` batendo exato). Causa: `projetarSerie` incluía **todo** futuro independente de estado — a saída de R$ 900 realizada dia 20 (movimento já no saldo) era subtraída de novo. Correção em `saldoProjetado.js`: projeção soma **só `previsto`** (realizado de conta está no saldo, de cartão está na fatura). Suíte 24/24 (novo T15 com o cenário real), build ok.

## 3. Renda comprometida: base = entradas da semana + saldo da semana anterior

Com saldo baixo o card estourou em 5451% (base só R$ 18,09). Nova regra em `planejamentoCalc.js`: modo atual = **saldo no fim do dia anterior ao início** (reconstruído via `calcularSaldoReal`, novo `saldoAnterior()` no hook) **+ todas as entradas do período** (realizadas ou previstas, sem duplicar). Validação real: 250,41 + 2400 = 2650,41 → **34%**. Suíte 12/12 (TESTE 11 do cenário real). `fechado`/`futuro` intactos.

## 4. Recebido parcial no relatório e na planilha (molde "50% do período")

O relatório lia `p.valor` (2400) quando só R$ 1.200 entraram (movimento `78702aee`). Correção em `relatorioRecebidoHoras.js` + hook: mapa `valorRealPorLancamento` (paralelo ao de data do Bug 2) — com `lancamento_id`, total/buckets/linha/extras usam a **movimentação**; sem, vale o previsto. Extra proporcional: `1200 − 1650 × 50% = R$ 375` (50% dos 750 cheios); sem divergência a fórmula é idêntica à anterior. Linha mostra "referente a 50% do período de 07/09/26 a 13/09/26" (integral mantém o texto antigo).
Setembro real: total 6580 → **5380** (2130 + 2050 + 1200). Suíte 38/38, build ok.

## 5. Sync da planilha com valor real

`gerar_recebidos_planilha.mjs` com a mesma correção (valor + descrição com %). A linha errada (`18/09 · R$ 2.400`, backup antes em `..._bak_antes_remocao.xlsx`) foi removida via COM e o bat rodado de novo: `18/09 · R$ 1.200 · 50%`, `B=825 / C=375 / F=1200`.

## Verificação

- Suítes: pendenciaAtraso 6/6, planejamentoCalc 11/11, saldoProjetado 24/24, rendaComprometida 12/12, relatorioRecebidoHoras 38/38, reconciliacaoPonto 19/19, planejamentosSerie 40/40, relatorioPdf 22/22. Build 376 ok.
